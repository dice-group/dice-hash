name: Detect POBR diff

on: [ pull_request ]

concurrency:
  group: detect-pobr-diff-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  detect-pobr-diff:
    uses: dice-group/cpp-conan-release-reusable-workflow/.github/workflows/abi-diff.yml@v2.0.1
    with:
      os: ubuntu-24.04
      setup_tools: true
      cc: clang-17
      cxx: clang++-17
      cmake-version: 3.24.0
      conan-version: 2.21.0
      base-branch: ${{ github.base_ref }}
      search-path: >
        include/dice/hash/internal
        include/dice/hash/blake
        include/dice/hash/lthash
      abi-version-header: include/dice/hash/version.hpp
      abi-version-const: dice::hash::pobr_version
    secrets:
      CONAN_CICD_USERNAME: ${{ secrets.CONAN_CICD_USERNAME }}
      CONAN_CICD_PASSWORD: ${{ secrets.CONAN_CICD_PASSWORD }}
      CONAN_CICD_ENDPOINT: ${{ secrets.CONAN_CICD_ENDPOINT }}